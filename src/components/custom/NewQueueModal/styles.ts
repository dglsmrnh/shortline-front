
import styled from "styled-components";
import {
  CModal,
  CModalHeader,
  CModalBody,
  CModalFooter,
  CButton,
  CForm,
  CFormLabel,
  CFormInput
} from '@coreui/react';


export const ModalHeader = styled(CModalHeader)`

`;

export const ModalBody = styled(CModalBody)`

`;

export const ModalFooter = styled(CModalFooter)`

`;

export const ModalButton = styled(CButton)`

`;

export const Form = styled(CForm)`

`;

export const FormLabel = styled(CFormLabel)`

`;

export const Input = styled(CFormInput)`

`;
